server:
  port: 8010

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/WojciechGalecki/microservices_spring_cloud_configuration
          username:
          password:
          clone-on-start: true
          skipSslValidation: true